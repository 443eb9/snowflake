import { makeStyles } from "@fluentui/react-components";

export const darkenContentStyleHook = makeStyles({
    root: {
        "backgroundColor": "#00000020",
        "padding": "4px",
        "borderRadius": "4px",
    }
})
